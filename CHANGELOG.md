# 更新日志

本项目的所有重要变更将记录在本文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
更详细的版本说明见 [docs/releases/](docs/releases/)。

## [0.3.0] - 2026-08-27

围绕 **前端可用性** 与 **状态/版本正确性** 的一次大规模修复与打磨。

### 新增

- **设计状态恢复**：切换会话或刷新页面后自动恢复 spec、版本列表、CAD 模型、
  设计检查/性能/气动/指标（此前 CAD 预览停留在"等待生成模型"、参数面板空白）。
- **聊天运行卡片持久化**：刷新/切换会话后，历史生成消息保留完整的
  "查看模型 / 深度设计探索 / 导出报告 / 查看运行细节"操作与 Job 详情。
- **Markdown 设计报告导出**："导出报告"下载排版完整的报告（参数表、设计检查、
  性能估算、气动扫描、设计指标与免责声明），替代原先打开原始 JSON。
- **版本芯片缩略图**：版本条中每个版本渲染俯视轮廓小图，配合两两对比与跨方案对比抽屉。
- **气动数据可视化**：气动分析面板新增 CL–α 与 L/D–α SVG 曲线图（仅绘制正 L/D 段，
  标注最优点），与数值表并列。
- **CAD 查看器视角工具栏**：等轴/俯视/前视/侧视一键切换。
- **会话管理增强**：搜索过滤、日期分组（今天/最近 7 天/最近 30 天/更早）、
  空标题回退"未命名会话"。
- **环境自检脚本** `scripts/check_openvsp_env.py`：一键校验 OpenVSP 安装、
  Python 绑定、生成链路与产物完整性。

### 修复

- **版本上下文丢失**：参数修改现在基于当前查看的版本打补丁
  （`PATCH /designs/{id}/spec` 新增 `base_version`），不再错误地变基到最新版本
  （旧版本上编辑会静默丢失版本语义）。
- **生成计时**：完成/失败后运行头部显示真实总耗时（此前可能显示 0.0s）。
- **设置弹层**：支持点击外部与 Escape 关闭（此前常驻遮挡右侧面板）。
- **底部抽屉**：设计检查/性能估算/气动分析/设计指标改为手风琴互斥展开，
  不再堆叠成遮挡工作区的"内容墙"。
- **"查看模型"反馈**：模型已显示时点击给出 CAD 面板高亮，而非毫无反应。
- **求解状态可辨识**：气动徽章区分 VSPAERO 成功 / 未运行 / 失败 / 模拟数据，
  模拟结果不再显示为绿色成功态。
- **布局徽章**：常规布局叠加机翼平面形状标注（如"常规布局 · 三角翼"）。
- **参数面板中文化**：裸露的键名与枚举值（planform、rear_fuselage、v_tail、endurance 等）
  提供中文标签；翼型型号保留原样。
- **响应式布局**：1440/1280/1120 分级收窄固定面板；≤960px 转为可用的纵向堆叠布局
  （此前 CAD 预览被压至约 200px 宽、窄屏完全散架）。
- **空状态引导**：参数面板与 CAD 预览空态增加操作指引文案。

### 变更

- **架构深化重构**（净约 -600 行，详见 `refactor/spec` 系列提交）：
  意图分类统一为 `IntentClassifier`；CAD 布局分派改为 `LayoutPlan` 数据驱动；
  移除 shadow mode 子系统（`/chat/shadow` 端点、ShadowLogger、shadow 图节点）；
  `pre_fill_none_scalars` 收敛至 `spec_patch` 公共模块。
- VSPAERO 分析默认开启（仍可通过设置面板或 `RUN_VSPAERO_ANALYSIS` 关闭）。
- 前端测试套件扩展至 177 项；后端全量 637 项。

## [0.2.0] - 2026-05-26

扩展气动布局与分析能力（完整说明见
[docs/releases/v0.2.0.md](docs/releases/v0.2.0.md)）：

- 新增 7 种气动布局（共 11 种），布局感知的 CAD 几何分派。
- VSPAERO 多表面分析、布局 QA 基础设施与验证脚本。
- 前端布局徽章、深度设计的布局感知策略。

## [0.1.0] - 2026-05

首个可用版本（说明见 [docs/releases/v0.1.0.md](docs/releases/v0.1.0.md)）：

- FastAPI 后端 + Next.js 14 前端 + LangGraph 设计代理的概念设计工作台。
- 聊天驱动的飞机设计生成（规格推导 → 校验补全 → CAD 生成 → 报告），
  fake CAD 后端，任务需求/机身/机翼/尾翼/动力参数编辑。
- 深度设计（AI 变体探索）、方案对比、对话持久化与版本管理雏形。

[0.3.0]: https://github.com/zweien/aero-spec-agent/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/zweien/aero-spec-agent/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/zweien/aero-spec-agent/releases/tag/v0.1.0
